import React, { Component } from "react";

export default class Donation extends Component {
    render() {
        return(
            <form>
                <h1>This is the Donations screen</h1>
            </form>
        );
    }
}