import React, { Component } from "react";

export default class PlayerStats extends Component {
    render() {
        return(
            <form>
                <h1>This is the PlayerStats screen</h1>
            </form>
        );
    }
}